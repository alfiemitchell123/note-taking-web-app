services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: luxor
      MYSQL_DATABASE: note_taking_app
      MYSQL_USER: admin
      MYSQL_PASSWORD: luxor
    networks:
      - mynetwork

  app:
    build: .
    networks:
      - mynetwork
    depends_on:
      - db

networks:
  mynetwork:
    driver: bridge
